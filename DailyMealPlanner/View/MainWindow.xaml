<Window x:Class="DailyMealPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cmp="clr-namespace:DailyMealPlanner.View.Components"
        xmlns:vm="clr-namespace:DailyMealPlanner.ViewModel"
        xmlns:local="clr-namespace:DailyMealPlanner"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000">

    <Window.DataContext>
        <vm:MainWindowVM/>
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*"/>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="0.4*"/>
        </Grid.ColumnDefinitions>

        <cmp:UserInfoCMP DataContext="{Binding CategoriesVM}" 
                         Grid.Column="0"/>
        <cmp:CategoriesCMP DataContext="{Binding UserInfoVM}" 
                           Grid.Column="1"/>
        <cmp:MealTimeEditor DataContext="{Binding MealTimeEditor}" 
                            Grid.Column="2"/>

    </Grid>
    
</Window>
